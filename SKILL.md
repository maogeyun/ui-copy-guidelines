---
name: ui-copy-guidelines
description: >-
  按对公/个人界面用语设计规范生成、检查、优化中文 UI 文案；可从截图或页面 URL 抽取可见文案后校验，
  并输出带图上编号标注的 HTML 检查报告（含弹窗/Toast/下拉等交互态）。
  Use when writing or reviewing 界面文案、用语规范、按钮/弹窗/Toast/输入框/空状态文案，
  when the user mentions 对公、财资、外汇、网银、投行、易收宝、个人APP、美好生活、容易贷、直接贷，
  or when extracting copy from screenshots or a live page URL, producing 截图标注 / HTML 报告 /
  文案检查报告 / 页面链接 / 线上文案 / 弹窗文案 / 交互态.
---

# 界面文案规范

按银行对公/个人《界面用语设计规范》生成与校验界面文案。规范细则不全部塞进本文件：先分流，再按需读取参考。

## 必读顺序

1. 判定产品线（对公 / 个人 / 未定）
2. 读 [shared-rules.md](shared-rules.md)
3. 只读对应分线：
   - 对公 → [corporate.md](corporate.md)
   - 个人 → [personal.md](personal.md)
4. 用词查 [glossary.md](glossary.md)
5. 产出格式对照 [examples.md](examples.md)
6. **读图检查**再读 [report.md](report.md)，用 `build_report.mjs` 出 HTML（截图必须 data URI 内嵌）
7. **页面链接检查**再读 [page-check.md](page-check.md)：打开 → 用户登录 → 抽取；不要用网格目测 bbox

禁止两份分线全文同时读入。未定时先问一句；截图无法判断则两边都检，冲突项单独标出。

## 产品线判定

| 线索 | 产品线 |
|------|--------|
| 财资、外汇、网银、投行、零售&易收宝、贵公司、B 端、财务管理专家 | 对公 |
| 个人 APP、美好生活、容易贷、直接贷、支付/退款/提现、个人财富专家 | 个人 |

称谓：规范允许按产品人格选「您」或「你」。用户未指定时，对公默认「您」，个人默认「您」；同一界面内不得混用。

## 工作流选择

| 用户输入 | 走哪条 |
|----------|--------|
| 现有文案、稿面标注、要改这句话 | 检查优化 |
| 需求/场景/组件，要写文案 | 按需求生成 |
| 截图、设计稿图、界面照片 | 读图检查 |
| http(s) 页面链接、线上/预发 URL | 页面链接检查 |

### 1. 检查优化

```
Task Progress:
- [ ] 判定产品线并加载对应参考
- [ ] 识别每条文案的组件与场景
- [ ] 按检查清单逐条过
- [ ] 输出问题表 + 可直接替换的改写稿
```

检查清单（每条过一遍）：

- [ ] 语气符合人格；无流行语、过可爱、过技术化、华丽修辞
- [ ] 无开发报错码、无生僻技术词；专有名附近有注解
- [ ] 短句、先说重要信息；相邻句不重复
- [ ] 少用「失败」「错误」；无双重否定、无无谓道歉
- [ ] 标题与说明呼应；同界面用词/视角统一
- [ ] 标点、数字、金额、时间、脱敏符合 [shared-rules.md](shared-rules.md) 与分线细则
- [ ] 动词/名词/近义词落在 [glossary.md](glossary.md)
- [ ] 按钮/占位/Toast 等符合下方组件句式

### 2. 按需求生成

```
Task Progress:
- [ ] 判定产品线、组件、用户目标、不可逆后果
- [ ] 用组件句式起草 1 条主方案（取舍大时加 1 条备选）
- [ ] 对照检查清单自检
- [ ] 交付终稿，注明人格与称谓
```

生成原则：少谈卖点、多谈用户要完成的事；正确时机给刚好够决策的信息。

### 3. 读图检查

主交付物是 **HTML 报告**（图上编号框 + 右侧结果与有依据的优化），不是聊天里的大表。流程见 [report.md](report.md)。

```
Task Progress:
- [ ] 复制原图到报告目录；`measure_image.py` 量宽高；`make_grid.py` 出网格后 Read 网格图
- [ ] 抽出全部可见文案，按从上到下、从左到右编号，写 **bbox_px**（禁止用聊天预览目测 %）
- [ ] 逐条过检查清单；需优化项写齐 rule / ruleQuote / why / suggested / how
- [ ] 若为复检：先核上一份 issue；无原图新证据不得改 pass
- [ ] 写出 report-data.json；`node scripts/build_report.mjs --data … --out copy-review-….html`（内嵌截图）
- [ ] 打开报告确认截图可见，不是「截图无法加载」
- [ ] 聊天只给摘要：条数、通过/需优化/严重、严重项一句、报告路径
```

看不清标 `unreadable`，不编造。通过项也编号上图。**禁止臆造图上没有的字。**

### 4. 页面链接检查

用户给出页面 URL 时走本条，不要当读图检查。流程见 [page-check.md](page-check.md)（三步：打开 → 用户登录 → 抽取）。

```
Task Progress:
- [ ] 判定产品线；视口对公 1440x900、个人 H5 375x812
- [ ] Cursor 用 MCP 打开/复用标签；否则 CLI：capture_page.mjs --headed --wait-login
- [ ] 仅首次判定为登录页时：请用户在刚打开的窗口登录，回复「继续」；不要要密码、不要换窗口
- [ ] 「继续」后直接抽当前页出报告；禁止再 navigate、禁止第二轮登录校验；仅仍有可见密码表单才再请一次
- [ ] assert_business_page.mjs 通过后再出报告（明确业务产物可覆盖历史 loginBlocked 误标）
- [ ] 用 capture.json 的 original + bbox_px 过检查清单（含弹窗/Toast/下拉各 state）
- [ ] 填 screenshots[] + screenshotId；`build_report.mjs --capture "$OUT/capture.json"` 绑定本批状态后出 HTML
- [ ] 打开报告确认截图、检查条目和编号框逐项对应
- [ ] 聊天摘要：URL、是否等过登录、状态数、弹层数、条数与严重项、报告路径
```

无 URL 仍走读图检查。链接检查不要跑 `make_grid.py`。

**判定只对规范 + 原文/原图。** 用户说「重新检查 / 再生成 / 再跑一遍」只表示再走本 skill，不表示上次结论有错，不得据此放宽、质疑或改判。复检可补漏项、改正 bbox 与流程；**无原图新证据不得把已成立的 issue 改成 pass。** 细则见 [report.md](report.md)。

**标点/空格禁止信 OCR。** OCR 常吞空格、把半角 `()` 认成全角 `（）`。括号空格、全半角、冒号须裁切原图放大（或沿文字行扫像素）后才定论。**禁止**只因 OCR 结果撤回已成立的标点/空格 issue。个人正例 `0.15%(原1.5%)`，反例 `0.15% (原1.5%)`。

## 检查输出

**纯文本检查 / 生成**（无截图）：先给问题表，再给可替换改写。

| 位置/组件 | 原文 | 问题 | 依据 | 建议 |
|-----------|------|------|------|------|

- **依据**：写条款名，如「对公·感叹号」「共用·请稍候」「语料·登录」
- 无问题的条目不列
- 改写稿按界面结构给出，可直接粘贴

**读图检查 / 页面链接检查**：不要用上面的表当终稿。输出 HTML，聊天摘要即可。字段见 [report.md](report.md)；链接检查另见 [page-check.md](page-check.md)。

生成任务输出：按组件列出终稿；文首一行注明 `产品线 / 人格 / 称谓`。

## 组件句式

文档无成章应用实例。下列句式由规范范围中的场景 + 写作原则推导；用词仍以 glossary 为准。

### 按钮

- 动词，或动词+对象；优先 2–4 字
- 无句号、无感叹号
- 线性流程下一步用具体动作（提交、下一步、去支付），不用「确定」硬套
- 对公常见：新增、删除、提交、审批、撤回/撤销、下载、打印、编辑、查看
- 个人常见：购买、查看更多、去支付、同意并继续、提交

### 二次确认弹窗

- 标题：动作后果或对象+动作（如「删除该笔指令」），无句号
- 正文：不可逆影响；单句可无句号，含逗号的多句须有句号
- 主按钮 = 确认动词（删除/提交/退出）；次按钮 = 取消
- 严肃场景可用「删除后数据将无法恢复」一类直述

### 失败 / 系统通知弹窗

- 先原因，再下一步；禁止把报错码当正文
- 避免「失败」「错误」作标题；改「未能{动作}」「暂无法{动作}」
- 网络类：说清现象 + 可执行动作（请检查网络后重试）

### 输入框

- 占位：`请输入{字段名}`，无句号
- 引导：说明格式/限制，短句，无句号
- 报错：缺什么或哪条规则 + 怎么改。例：「请输入 11 位手机号」

### 警告提示

- 风险在前，动作在后
- 对公严重警告才可用感叹号；个人电商兴奋场景也可用（见分线）

### Toast / 全局提示

- 成功：`{对象}{动作}成功` 或 `{动作}成功`
- 失败：原因 + 下一步；短、可扫视
- 操作中：`{动作}中`
- 说明/名词解释：一句说清，不堆概念

### 加载

- 页面/批量：`{动作}中` 或「请稍候」
- 「请稍后」后必须有后续成分（请稍后重试）
- 个人另有：下拉刷新、上滑加载、底部点击加载

### 上传

- 帮助：格式、大小、数量限制
- 失败：哪条限制未满足 + 怎么改，不暴露系统码

### 卡片 / 页面标题（对公）

- 标题与当前步骤一致：录入 / 确认 / 详情
- 标题中不宜出现「的」

### 复选框 / 协议

- 协议勾选：`我已阅读并同意「{协议名}」`
- 产品勾选：对象名清晰，不跟协议句式混用

### 页面中心 / 空状态

- 现状（无数据/未开通/暂无结果）+ 下一步动作
- 鼓励尝试时可用「仅需」「简单」「轻松」「即可」；对公用「只需」

### 标签 / 订单状态 / 库存（个人）

- 标签：2–4 字状态或利益点，无句号
- 订单：待付款、支付中、待使用、退款中、退款成功、交易成功、关闭订单、券码过期
- 库存：未开售、已开售、已售罄

### 功能图标

- 图标必有可理解短标签；与 glossary 动词一致

## 硬性禁止

- 界面直接展示报错代码、堆栈、字段名（如 `null`、`error_code`）
- 同屏混用近义词（确定/确认、创建/新建、删除/移除）
- 对公正文滥用感叹号；个人非生活化场景滥用感叹号
- 编造规范里没有的产品专名或法律承诺
- 读图时补写未出现的营销句
- 因用户要求重新检查/再生成就质疑或撤回已按规范成立的结论
- 用 OCR、压缩预览或「这次重新跑要更谨慎」替代 skill 规定的证据去改判
- 页面链接检查中换窗口验登、用户说「继续」后仍 navigate，或向用户要密码
- 用无痕 / open_resource / 系统 Chrome 再登一次来「验证」登录
- 登录后还做第二轮登录墙探测，或因 URL 含 /login、残留「登录」链再次要求登录
- 在明确登录表单产物 / login.png 上生成业务文案报告（最终 default.png 已是明确业务签名时除外）
- 只因 URL 含 /login 就把业务列表判成登录墙
- 交付只含 `default.png` 相对路径、未用 `build_report.mjs` 内嵌截图的 HTML 报告
- 页面链接报告未传 `--capture`，或借用其他抓取批次的同名截图补图
