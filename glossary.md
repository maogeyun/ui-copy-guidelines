# 语料库

范围：`共用` 两边都用；`对公` / `个人` 只在对应产品线强制。冲突时分线优先。

## 名词

| 正 | 不要用 | 范围 |
|----|--------|------|
| 账号 | 帐号 | 共用 |
| 抱歉 | 对不起 | 共用 |
| 电子邮箱 | Email 邮箱、邮箱、Email | 共用 |
| 即时消息 | 及时消息 | 共用 |
| 提醒方式 | 提醒渠道 | 共用 |
| 偏好 | 喜好 | 共用 |
| 模板 | 模版 | 共用 |

个人补充：一个用户对应一个**账号**；账户是账号的资金容器。未确认业务定义前，登录身份用「账号」，余额/卡片用「账户」，同屏不要混着指同一对象。

## 动词（指令动作）

| 统一用语 | 英文 | 定义 | 不要用 | 范围 |
|----------|------|------|--------|------|
| 登录 | Login | 身份验证后进入系统 | 登陆 | 共用 |
| 删除 | Delete | 对象从当前场景移除且被销毁 | 删掉、清除 | 共用 |
| 清空 | Clear | 输入项恢复初始 | 清除 | 共用 |
| 关闭 | Close | 关闭当前窗口 | — | 共用 |
| 取消 | Cancel | 取消当前操作并关闭窗口 | — | 共用 |
| 完成 | Done | 结束当前场景工作并返回上一场景 | 完结、结束 | 共用 |
| 启用 | Initiate | 使对象开始执行任务 | 开启、开始使用 | 共用 |
| 停用 | Deactivate | 使对象停止执行任务 | 禁用、关闭、关掉 | 共用 |
| 配置 | Customize | 按偏好配置功能 | 自定义 | 共用 |
| 应用 | Apply | 不关闭窗口使配置生效 | 启用、完成 | 共用 |
| 还原 | Restore | 删除或损坏后恢复数据 | 复原 | 共用 |
| 重置 | Reset | 回到上次保存/Apply 的值，不关窗 | 还原 | 共用 |
| 导入 | Import | 从外部引入，通常要用户定位 | 倒入、插入 | 共用 |
| 导出 | Export | 导出为外部可用格式 | 倒出 | 共用 |
| 插入 | Insert | 在特定位置插入元素 | 倒入 | 共用 |
| 分享 | Share | 分享到外部程序 | — | 共用 |
| 管理 | Manage | 系统化控制与维护 | — | 共用* |
| 查询 | Query | 按条件主动获取信息 | — | 对公 |
| 暂存 | Temporary storage | 临时保留待后续处理 | — | 对公 |
| 激活 | Activate | 触发流程或从挂起转为可执行 | — | 对公 |
| 校验 | Verification | 按规则验证完整、准确、合规 | — | 对公 |
| 签收 | Receive Confirmation | 电子确认已接收并认可状态 | — | 对公 |
| 同意 | Assent | 流程申请的正式批准 | — | 对公 |
| 拒绝 | Reject | 常规、可修正的驳回 | — | 对公 |
| 关联 | Related | 描述数据元素之间的关联 | — | 对公 |
| 新建 | Create | 从无到有且需一系列输入或配置 | 创建、添加、新增（作同一按钮时） | 个人 |
| 确认 | OK | 确认当前操作并关闭窗口 | 确定（作该按钮时） | 个人 |
| 解除 | Remove / Cancel | 终止关联、绑定、订阅；源对象仍在 | 删除（若对象不应消失） | 个人 |

\*「管理」两边都有，定义侧重不同：对公是对系统对象的控制维护；个人是对账户/设置/订阅/数据的自主增删改查。

## 「请」

具体某次行为可用「请」。列举时不宜连续出现「请」。

正：`账号验证失败，请重新登录。`
反：`账号验证失败，重新登录。`

正（列表）：
- 保持设备使用最新版本的软件。
- 不使用 WLAN、GPS 和蓝牙功能时将它们禁用。

反：每条都以「请」开头。

## 代词 / 助词

| 正 | 不要用 | 范围 |
|----|--------|------|
| 其他 | 其它 | 共用 |

「的」后接名词；「地」后接动词；「得」后接形容词。一句内避免连续「的」。标题中不宜出现「的」。

正：`连续快速地拍摄多张照片` / `跑得快`
反：`连续快速的拍摄多张照片` / `跑的快`

## 易错词

| 标准 | 不推荐 | 说明 | 范围 |
|------|--------|------|------|
| 登录 | 登陆 | — | 共用 |
| 请稍候 | 请稍后 | 单独使用用「请稍候」；「请稍后」后须有其他成分 | 共用 |
| 账号 / 账户 | 混用同一对象 | 个人：用户是账号；资金容器是账户 | 共用 |
| iOS | IOS、ios | — | 共用 |
| App | APP | 普通名词缩写，不是首字母缩写 | 共用 |
| 模板 | 模版 | — | 共用 |

## 近义词

同一界面只选一个，选后全文统一。

### 确定 & 确认

- **确定**：肯定、断定，强调结果。对公示例按钮可用「确定」。
- **确认**：辨认、审核，强调过程。对公：`确认发送`；个人：`确认密码`；个人关闭对话框的主按钮用「确认」不用「确定」。

### 创建 & 新建

- **创建**：创立原本不存在的事物；或已有产品类型上的大颗粒复杂体系。例：创建意图、创建类目树。
- **新建**：基于已有事物或规则建立中等颗粒度对象。例：新建文档。
- 个人按钮统一用语是「新建」，不要和创建/添加/新增混作同一操作名。

### 新增 & 添加

- **新增**：新增加一种已知类型、小颗粒内容。例：新增节点。
- **添加**：把已有内容拿过来建立链接。例：添加数据、添加成员。
- 二者都用于小粒度对象，同产品保持统一。

### 删除 & 解除 / 移除

- **删除**：操作后对象从系统中消失。
- **解除 / 移除**：解除引用关系，源目录中对象仍在。个人统一用语「解除」。

## 专业词索引

- 对公交易/外汇/票据：见 [corporate.md](corporate.md)
- 个人支付/贷款：见 [personal.md](personal.md)
