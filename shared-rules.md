# 共用规则

对公、个人共用。分线冲突以 [corporate.md](corporate.md) / [personal.md](personal.md) 为准。

## 写作原则

1. **专业、尊重**：像专家交谈，避免正式到机械、晦涩或傲慢。不要完全按口语写，以免冗长。
2. **支撑用户目标**：少谈卖点、多谈买点。正确位置、正确时机，信息不多不少。重要的先说，次要的后说，不重要的不说。
3. **短句**：上下文已知的不重复；相邻句不相互复述；删多余字。
4. **易懂**：常用词优先；业务专名附近给注解；不把开发语言暴露给用户。
5. **鼓励、避免负向**：自信说出用户能做什么。少用「失败」「错误」。避免双重否定、道歉语气。
6. **逻辑连贯**：标题与说明呼应；同界面用词、风格、视角统一。

鼓励词：个人用「仅需」「简单」「轻松」「即可」；对公用「只需」等同义（见分线）。

## 语气语调

听起来专业、直白、精干。朗读时若生硬拗口，改为更对话、仍书面的表达。

应避免：流行语、太可爱、太轻浮、太技术化、华丽修辞、诋毁任何群体（含竞品）。

感叹号规则见分线（对公更严，个人电商场景可放宽）。

语气示例：

| 语气 | 示例 |
|------|------|
| 平和 | 可通过应用中心探索更多应用 |
| 安抚 | 上传时间稍长，请耐心等待 |
| 严肃 | 删除后数据将无法恢复 |

## 标点

### 句号

下列元素**单独出现时省略句号**：标签、标题、输入框下提示、悬停提示、表格短句、文字链或文字按钮后面。

弹窗/尖角气泡：中间用了「，」，结尾就要「。」。个人补充：一段话可以不用句号；两段且中间有逗号则结尾要句号。

下列情况**需要句号**：多句或多段文案和列表；文字链前的完整句子。

正：`请输入应用名` / `删除后数据将无法恢复`（单句弹窗）
反：`请输入应用名。` / `删除后数据将无法恢复。`（单句却加句号）

正：`所选国家为一类涉敏国家，不允许发起汇款申请。`
反：同上但无句号

正：`创建你的第 1 个产品。详情`
反：`创建你的第 1 个产品，详情。`

### 千分位

金额默认千分位：`123,456` 而非 `123456`。电商商品价格可不加。

### 感叹号

默认不用。分线例外见 corporate / personal。

### 括号

半角括号 `()`。序号加括号如（一）后不再加顿号等标点。

括号与汉字之间的空格：**对公、个人写法不同**，见分线。

### 分隔号

- 百分比区间：前后都加 `%`，中间用 `-`（个人也可用 `～`）：`30%-50%`
- 电话/门牌：`010-88842603`，减号两侧无空格
- 天数/时段：`4-5天`、`14:00-15:00`；不用 `2:00 pm - 3:00 pm`
- 日期分隔符见分线

### 冒号

「表示」「认为」「强调」「证明」等后、语气郑重时用冒号。否则用逗号。比例用半角：`16:9` 而非 `16：9`。

### 顿号

「首先」「其次」「第一」后用逗号，不用顿号。书名号、引号连续使用，中间不加顿号：`《安全法》《交通法》`。

描述产品时不用「最佳」「最好」「最著名」等强烈评价词。

### 引号

应用名、功能名、按键名、业务名加引号：`「设置」功能`、`「确认」键`。

完整独立引语，标点在引号内；引语作为句子成分，标点在引号外。业内习语、特定称谓加引号（「一参一控」「两会」等）。

界面优先用中文弯引号「」或规范中的 “”；同一产品内统一。

## 数字

### 必须用阿拉伯数字

物理量（高 30 米、重 80 千克）；计数计量与统计（80%、1:3）；量词（50 条、30 天）；版次卷次页码；文件编号与序列号；公元世纪年月日时刻。

### 必须用汉字

相邻数字并列（三五个、十五六岁）；专有名词（二氧化碳）；顺序修饰（第一次、第一季度）；成语惯用语；节日与特定事件（五一劳动节）；干支纪年与夏历（正月初一）。

### 分节展示

| 类型 | 格式 | 示例 |
|------|------|------|
| 银行卡号 | 四位一空 | 1234 1234 1234 1234 123 |
| 身份证号 | 六四四四 | 620121 1998 1112 1234 |
| 手机号 | 三四四 | 186 1111 2903 |

## 货币

- 金额用阿拉伯数字
- 资产额度（个人含支付金额）保留两位小数
- 同一页面小数位一致
- 格式：`货币符号+数字`，如 `¥123.00`；多币种建议 ISO 代码 `CNY123.00`

三种场景：

| 场景 | 写法 |
|------|------|
| 用户输入金额 | ¥500.00 |
| 查看流水 | 500.00 |
| 强调进出 | +500.00 / -500.00 |

常用符号：USD `$`、EUR `€`、JPY `¥`、GBP `£`、CNY `¥`；CAD/AUD/NZD/SGD 用 `$` 或带前缀（C$、A$、NZ$、S$）。CHF 无统一常用符号时写币种名或 ISO 代码。

## 时间（共用骨架）

公元世纪、年月日、时刻用阿拉伯数字。完整句子里可用「3 月 18 日」；孤立信息可用 `03-18`。

时分秒与日期搭配：`2019-07-23 10:05:05`（日期与时间之间空格，不用 `-` 连）。`yyyy-MM-dd HH:mm:ss`：`MM` 为月，`HH` 为 24 小时制。

日期分隔符、是否优先绝对时间、相对时间档位 → 见分线。

## 脱敏

全部脱敏用一组 `****`；部分脱敏保留真实位数，隐藏位用 `*`。

| 类型 | 规则 | 示例 |
|------|------|------|
| 姓名 2 字 | 留首字 | 张* |
| 姓名 ≥3 字 | 留首尾 | 张*妮 / 张**妮 |
| 手机 | 前 3 后 4 | 186****1402 |
| 身份证 高 | 前 1 后 1 | 6******************2 |
| 身份证 中 | 前 3 后 3 | 213************203 |
| 身份证 低 | 前 6 后 4 | 212912******2233 |
| 地址 | 留省市区 | 浙江省宁波市 江北区 **** |
| 银行卡 高 | 仅后 4 | **** 1208 |
| 银行卡 中 | 前 4 后 4 | 6201 **** **** 1208 |

邮箱、企业名称 → 见分线。

## 全角半角与大小写

英文整句、特殊名词内用半角标点：`Stay hungry, stay foolish.` 不用中文逗号句号。

专名大小写正确：JavaScript、HTML5、React；不要写成 Js、h5、RJS。

## 「请」、代词、助词

- 单次行为可用「请」：`账号验证失败，请重新登录。`
- 列举时不要每条都「请」
- 「其他」不用「其它」
- 「的」接名词，「地」接动词，「得」接形容词
- 一句内避免连续「的」；标题中不宜出现「的」
